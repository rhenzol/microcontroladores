Classe que implementa a função de leitura e escrita da memória EEPROM interna do microcontrolador Atmega328pb no arquivos "eeprom.h" e "eeprom.cpp". 
Também contém um exemplo de uso da classe com operações simples de leitura e escrita no arquivo main.cpp.
Essa classe não implementa as escritas e leituras em interrupções.
